{
  "name": "sequelize-auditing",
  "version": "0.1.0",
  "description": "A plugin for sequelize to store data changed into another table",
  "main": "./lib/",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha --check-leaks --colors -t 10000 --reporter \"spec\"",
    "cover": "./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec test/all_test.js"
  },
  "keywords": [
    "sequelize",
    "sequelize-plugin",
    "auditing"
  ],
  "author": "Channer",
  "license": [
    {
      "type": "MIT",
      "url": ""
    }
  ],
  "devDependencies": {
    "chai": "^3.5.0",
    "istanbul": "^0.4.2",
    "mocha": "^2.4.5",
    "mysql": "^2.10.2",
    "sequelize": "^3.19.3"
  },
  "dependencies": {
    "lodash": "^4.5.1"
  }
}
